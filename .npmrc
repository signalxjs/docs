# Allow internal @sigx packages to be hoisted so TypeScript can resolve types
# (some packages export source from src/ and reference each other via subpath
# exports — public-hoist-pattern keeps those resolvable from docs's tsconfig).
public-hoist-pattern[]=@sigx/*
